{# 
===============================================================================
Archivo generado automáticamente por Ansible (template .j2). 
No editar directamente, los cambios se perderán en la próxima ejecución.
===============================================================================
#}
global:
  scrape_interval: 30s
  evaluation_interval: 25s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
  - job_name: "metric_station"
    static_configs:
{% for station in metric_stack_stations %}
      - targets: ["{{ station.ip  }}"]
        labels:
          Fila: "{{ station.fila }}"
          Name: "{{ station.name }}"	
{% endfor %}
